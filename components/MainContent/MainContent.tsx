import React from 'react'

type Props = {}

const MainContent = (props: Props) => {
	return (
		<section className='h-full w-[40vw] bg-zinc-800'>MainContext</section>
	)
}

export default MainContent
